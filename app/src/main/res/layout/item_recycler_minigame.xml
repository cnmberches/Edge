<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@android:color/transparent"
    android:padding="4dp"
    android:clipToPadding="false">

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/home_card_mini_games"
        android:layout_width="240dp"
        android:layout_height="160dp"
        android:layout_marginTop="0dp"
        style="@style/CardViewDefaultStyle">

        <ImageView
            android:id="@+id/home_mini_games_img"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:src="@drawable/img_game_stress"
            android:scaleType="centerCrop"/>

        <View
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/scrim_dark_to_bottom"/>

        <TextView
            android:id="@+id/home_mini_games_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:text="Game title"
            android:fontFamily="@font/poppins_semibold"
            android:textColor="@color/colorOnDarkPrimary"
            android:textAppearance="@style/TextAppearance.MaterialComponents.Headline5"/>

    </com.google.android.material.card.MaterialCardView>

</FrameLayout>